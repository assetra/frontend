import Main from "@/components/ambassador/Main";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Assetra Ambassador Program",
};
const page = () => {
  return <Main />;
};

export default page;
