import Main from "@/components/business/Main";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Assetra For Businesses",
};

const page = () => {
  return <Main />;
};

export default page;
